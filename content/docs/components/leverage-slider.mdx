---
title: Leverage Slider
description: A slider component for selecting leverage levels
---

<ComponentDemo>
  <LeverageSlider defaultValue={[5]} min={1} max={50} step={1} />
</ComponentDemo>

```tsx
import { LeverageSlider } from "@/components/sol/leverage-slider";

export function LeverageSliderDemo() {
  return <LeverageSlider defaultValue={[5]} min={1} max={50} step={1} />;
}
```

## Installation

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tab value="pnpm">pnpm dlx solanaui@latest add leverage-slider</Tab>
  <Tab value="npm">npx solanaui@latest add leverage-slider</Tab>
  <Tab value="yarn">yarn solanaui@latest add leverage-slider</Tab>
</Tabs>

## Usage

```tsx
<LeverageSlider defaultValue={[5]} min={1} max={50} step={1} />
```

## Source Code

<ComponentCode name="leverage-slider" />
